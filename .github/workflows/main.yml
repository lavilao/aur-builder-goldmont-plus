name: Build Zed Binaries

on:
  workflow_call:
    inputs:
      repository:
        required: true
        type: string
      ref:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      repository:
        description: 'Repository to build from'
        required: true
        type: string
      ref:
        description: 'Ref to build from'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [editor-opengl]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repository || github.event.inputs.repository }}
          ref: ${{ inputs.ref || github.event.inputs.ref }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies and mold linker
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            mold \
            libx11-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxcursor-dev \
            libxcb1-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            libx11-xcb-dev \
            libegl1-mesa-dev \
            libgles2-mesa-dev \
            libasound2-dev \
            libgtk-3-dev

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "zed-opengl"
          cache-on-failure: true

      - name: Build
        run: cargo build --release --bin zed
        env:
          RUSTFLAGS: "--cfg gles -C link-arg=-fuse-ld=mold -C --remap-path-prefix $PWD=/"
          CARGO_BUILD_JOBS: "4"
          CARGO_INCREMENTAL: "1"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: zed-${{ matrix.target }}-linux
          path: target/release/zed
